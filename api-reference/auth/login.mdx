---
title: Login
description: Authenticate a user with email and password
---

# Login

Authenticate a user with their email and password.

## Endpoint

```
POST /auth/login
```

## Request Body

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `email` | string | Yes | User's email address |
| `password` | string | Yes | User's password |

```json
{
  "email": "user@example.com",
  "password": "securePassword123"
}
```

## Response

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "user_abc123",
      "email": "user@example.com",
      "name": "John Doe",
      "createdAt": "2024-01-15T10:30:00Z"
    },
    "token": "eyJhbGciOiJIUzI1NiIs..."
  }
}
```

## Examples

### cURL

```bash
curl -X POST https://api.cocobase.cc/auth/login \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "securePassword123"
  }'
```

### JavaScript

```javascript
const response = await fetch("https://api.cocobase.cc/auth/login", {
  method: "POST",
  headers: {
    Authorization: "Bearer YOUR_API_KEY",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    email: "user@example.com",
    password: "securePassword123",
  }),
});

const result = await response.json();
const { user, token } = result.data;

// Store token for subsequent requests
localStorage.setItem("authToken", token);
```

### Python

```python
import requests

response = requests.post(
    "https://api.cocobase.cc/auth/login",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json",
    },
    json={
        "email": "user@example.com",
        "password": "securePassword123",
    },
)

result = response.json()
user = result["data"]["user"]
token = result["data"]["token"]
```

## Using the Token

After login, include the token in subsequent requests:

```bash
curl https://api.cocobase.cc/collections/posts \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..."
```

## Error Responses

### Invalid Credentials

```json
{
  "success": false,
  "error": {
    "code": "INVALID_CREDENTIALS",
    "message": "Invalid email or password"
  }
}
```

### User Not Found

```json
{
  "success": false,
  "error": {
    "code": "USER_NOT_FOUND",
    "message": "No user found with this email"
  }
}
```

### Account Locked

```json
{
  "success": false,
  "error": {
    "code": "ACCOUNT_LOCKED",
    "message": "Account temporarily locked due to too many failed attempts"
  }
}
```
